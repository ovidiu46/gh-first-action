name: First Workflow
on: workflow_dispatch
jobs:
	my-first-job:
 		runs-on: ubuntu-22.04
	 	steps:
	 		- name: Print greeting
				run: echo "Hello world!"
			- name: Print goodbye
	 			run: echo "Done - bye!"
